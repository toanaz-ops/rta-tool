# Sweep deconvolution and the impulse response — decisions

*2026-08-30. Lane L4a, station 2. Written from the station-1 research pass. The
sweep generator and its Farina inverse filter already exist and are tested
(`core/include/rta/gen/Sweep.h`). This record was written before any of it was
built, so the implementation could not silently choose otherwise.*

**BUILT 2026-08-30, in part.** Decisions 1-4 and 7-9 are implemented in
`core/src/ir/` and in `rta::gen::Sweep`; decision 6 (polarity) is **not built**.
Seventeen commits, `408d0a5..acaae9a`. Numbers live in `docs/HANDOFF.md`'s
baseline block and nowhere else. Four claims in this record were corrected by
being measured and two more by a reviewing session — the table below is the
record of that, kept deliberately rather than tidied away.

L4 as written in `docs/plans/MASTER-EXECUTION-PLAN.md` gathers ten features. It
is split here the way L5 had to be split:

| | scope | layer | blocked by |
|---|---|---|---|
| **L4a** | deconvolution → IR, frequency response from the IR, polarity (G21) | `core/` | — |
| **L4b** | ETC, Schroeder, Lundeby, EDT/T20/T30, C50/C80/D50 | `core/` | L4a |
| **L4c** | draggable IR gate (G25), min/excess phase (G24), offline WAV (G22) | `app/` | L4a, L4b |
| **L4d** | STI/STIPA (G4) | — | **purchase of IEC 60268-16** |

This record covers **L4a only**. L4b and L4c get their own; L4d is not scheduled.

Ten decisions follow. Each records what the rejected option would have cost.

**Every number here is reproducible by one of three committed commands**, none
of which writes anything:

```
.venv/Scripts/python.exe tools/probe_sweep_fade.py          # pulse geometry, fade law
.venv/Scripts/python.exe tools/verify_l4a_pulse.py          # sections A B C F I J
.venv/Scripts/python.exe tools/verify_l4a_measurement.py    # sections D E G H K L
```

The last two exist because the first draft of this record asserted several
things no measurement had been run for. **Six did not survive scrutiny, and are
marked in place rather than quietly corrected:**

| | what the draft said | what checking it said |
|---|---|---|
| dec. 1 | no closed form produces the regularisation term | **Kirkeby & Nelson 1999 does**, and `pyfar` ships it. The objection was overstated; the decision survives on narrower grounds |
| dec. 4 | a unity path reads 0 dB flat in band | **−7.06 … +2.40 dB** at the shipped fade widths, and the quoted band was computed at the wrong duration |
| dec. 5, 9 | the practice and the capture rule come from AES-2id | **AES-2id is about the AES3 digital interface.** The citation came from a web page nobody checked |
| dec. 6 | the confidence figure is how the caller finds out | it is the **highest** reading on the one case that answers wrongly |
| dec. 6 (the fix) | a symmetry gate at 1.0 dB separates them | a 28-cell survey says **no threshold on that figure works**; withdrawn, not tuned |
| dec. 7 | transform from `originIndex` | that slices the analysis pulse in half — **+23.5 dB** error on a highpass |
| dec. 8 | float32 and float64 agree | the comparison had been run in a way that could not have disagreed |

Two further claims were guesses the measurements then split down the middle:
decision 5's explanation of the fade asymmetry was **half confirmed and half
refuted**, and decision 8's refusal to locate the precision crossover turned out
to be over-cautious — there is none in reach.

**Three of these came from a reviewing session, not from this one.** The
AES-2id citation and the Kirkeby overstatement in particular would have shipped:
both read as authoritative, and neither was going to be caught by running the
scripts again. That is the case for a reviewer who reads the literature rather
than the diff.

A record that hides its own corrections teaches the next reader that this kind of
work does not produce any. Decision 5's central claim was also put deliberately
at risk — an experiment designed to kill it — and survived; that is recorded in
place too, because a decision nobody tried to falsify is a decision nobody
tested.

---

## 1. Linear deconvolution by convolution with an inverse filter

**Decision.** The impulse response is `y ⊛ inv`, a **linear** (non-cyclic)
convolution computed by FFT with zero padding to `nextPow2(Ny + Ninv − 1)`.
There is no spectral division anywhere in L4a.

**Why not spectral division — and the overstatement this paragraph used to
make.** Müller & Massarani's own recommendation is `IFFT(Y/X)` with both signals
zero-padded to double length; `spatialaudio/sweep`'s `deconv_process()` is
exactly `irfft(rfft(y, N) / rfft(x, N))`. `X` is near zero outside `[f1, f2]`,
so every such implementation needs a regularisation term.

A first draft of this record rejected the method on the grounds that the term is
"a number somebody picks — no standard defines it, no closed form produces it".
**That is wrong, and a reviewer who had read pyfar would have caught it.** The
recipe is Kirkeby & Nelson (JAES 47(7), 1999):

    S⁻¹(f) = S*(f) / ( S*(f)·S(f) + ε(f) )

with ε **frequency-dependent and derived from the sweep's own `[f1, f2]`** —
large outside the band, negligible inside, raised-cosine crossfaded across a
width of √2·f at each edge. `pyfar.dsp.regularized_spectrum_inversion` ships
exactly that, taking `frequency_range` as an argument with `regu_outside = 1.0`
and `regu_inside = 1e-10`. Farina himself moved to a Kirkeby stage after 2007.
So the shape of ε is principled, not arbitrary, and the original objection does
not stand as written.

**What survives the correction, stated at its true strength.** Two things, and
neither is "the other method is wrong":

1. `regu_inside` is still a chosen constant. −200 dB is a sensible default and
   nothing in this repo could measure whether it is the right one, so adopting
   the method would import one unfalsifiable number. Convolution with the Farina
   inverse imports none, because it never divides.
2. The Farina path is **already built and already tested here**
   (`Sweep::buildInverseFilter()`, `core/tests/test_generator_sweep.cpp`).
   Choosing it is choosing one stage over two, in a lane whose job is to reach a
   working impulse response.

This is a deferral, not a verdict. Decision 2 is what makes it cheap to revisit.

**What that costs, stated plainly.** Müller criticises the Farina construction
directly: deconvolving with only the time-inverted, amplitude-shaped stimulus
leaves errors near the start and end frequencies of the sweep, because the
sweep's switch-on and switch-off ripple the extremities of its spectrum. He is
right, and decision 5 turns his qualitative warning into a measured number and
a fix. His own remedy — invert a *measured* reference spectrum obtained by
looping the output back to the input — is better, and is not available: the
physical loopback cable is task **M7** in `docs/reports/T12-hardware-run.md`,
still waiting on a human. Decision 2 is what keeps that door open.

## 2. The inverse filter is an argument, never a construction

**Decision.** `Deconvolver` takes `std::span<const float> inverseFilter`. It
does not own a `Sweep`, does not include `rta/gen/Sweep.h`, and has no idea how
the filter was made.

**Why this is the most important line in the design.** Today the caller passes
`Sweep::buildInverseFilter()`. The moment M7's loopback cable exists, Müller's
reference-measurement method becomes available, and adopting it must be a change
of *argument*, not a change of code. A `Deconvolver` that constructed its own
filter internally would have to be rewritten to accept the better method — and
a class that must be rewritten to accept a known improvement is a class that
will still be there in a year, holding the improvement out.

The rejected alternative — `Deconvolver(const Sweep&)` — reads more convenient
at exactly one call site and forecloses the method the literature prefers.

## 3. Nothing is chopped, and the origin is stated rather than assumed

**Decision.** The result carries the **whole** linear convolution, plus the
index at which `t = 0` sits, plus `L`:

```
struct Deconvolution {
    std::vector<float> samples;      // length Ny + Ninv - 1, nothing removed
    std::size_t        originIndex;  // = Ninv - 1
    double             sampleRate;
    double             normalisationGain;  // the scalar already applied
    double             harmonicSpacingL;   // L = T / ln(f2/f1)
};
```

**Where `originIndex = Ninv − 1` comes from.** `inv[m] = s[Ns−1−m]·e[m]`. At
convolution lag `k = Ns−1`, every term of `Σ_j s[j]·inv[k−j]` becomes
`s[j]²·e[Ns−1−j]` — all non-negative, the one lag at which the sum adds
coherently. So the analysis pulse peaks at `Ns−1`, and since `Ninv = Ns`, that
is `Ninv − 1`. Measured, not assumed, by `tools/probe_sweep_fade.py`: peak index
95999 for a 2 s sweep at 48 kHz (`N−1 = 95999`), sign **positive**, and
symmetric about the peak to 3.0e−5 of peak amplitude over ±2 ms.

**Why nothing is chopped.** Müller's advice is to discard the negative-time
half. That is correct for room acoustics and wrong for this repo, because the
same region *is* the distortion measurement. The Nth harmonic packet sits at

    Δt_N = −L · ln(N),        L = T / ln(f2/f1)

which follows in closed form: the Nth harmonic's instantaneous frequency at time
`t` equals the fundamental's at `t + L·ln N`, so deconvolution — which maps the
fundamental at `t` onto zero — maps the harmonic onto `−L·ln N`. Müller's own
worked example agrees (a sweep at 100 Hz/100 ms and 200 Hz/200 ms puts H2 at
−100 ms, and `L·ln 2 = 100 ms`). Verified numerically against a square-law and
a cubic nonlinearity: for a 20 Hz–20 kHz sweep, **H2 lands at 0.0 samples of
error and H3 at −0.1**. Two orders rather than one, so the *logarithmic*
spacing is exercised and not merely a single offset that any monotone law would
have fitted.

**The residual is config-dependent, and a draft of this record hid that** by
quoting one configuration's figures as if they were the method's. For a
100 Hz–10 kHz sweep the same measurement gives **+2.44 samples for H2 and −1.18
for H3** — reproduced exactly by both the C++ and an independent NumPy model, so
it is physics, not an implementation error. The packets are sharp (a −3 dB half
width of about two samples), so this is a real systematic offset rather than
peak-finding noise. It is precisely what Novak's synchronisation condition
exists to remove, which is the qualifier below. Tests must therefore assert the
**logarithmic spacing** — the claim with teeth, since a wrong sign or base
misses by thousands of samples — inside a tolerance of a few samples, and must
not quote ±1 as if it held everywhere.

Discarding that region would make G3 (THD readout) and P4b re-measure what was
already captured. `harmonicSpacingL` is carried so a later lane needs no access
to the `Sweep` that produced the data.

**A qualifier this claim needs, and did not have.** `Δt_N = −L·ln N` locates the
packets to a fraction of a sample — H2 measured at 0.0 samples of error, H3 at
−0.1 — but the packets' **phase** is another matter. Novak's *synchronized*
swept-sine (EURASIP 2010; JAES 2015) requires

    L = round(T̂ · f1 / ln(f2/f1)) / f1,      i.e.  f1·L must be an integer

so that each harmonic's offset is a whole number of samples *and* its phase at
the packet is a multiple of 2π. Without that rounding — which changes the sweep
duration by well under 1% — each harmonic's frequency response carries a
frequency-dependent phase rotation. **Amplitude THD survives; phase-accurate
per-harmonic analysis does not.** L4a measures neither, so nothing here breaks;
but the constraint belongs on the record now, because it is a one-line change in
the generator today and a re-measurement of every stored capture later. Whether
to add it is listed as an open decision below.

## 4. Normalisation: one in-band scalar, with the band derived from the fades

**Decision.** `normalisationGain` is chosen so the mean of
`|FFT(sweep ⊛ inv)|` over the valid band equals 1. A unity path then reads 0 dB
in band **to within the flatness that band actually has** — which is a measured
quantity, not the zero this decision originally claimed.

**Why the band is not a chosen constant.** The fades are the reason the band
edges are unreliable, so the band is defined by where the fades have finished:

    f_lo = f1 · exp(fadeInSec / L)
    f_hi = f2 · exp(−fadeOutSec / L)

Every figure is read off the `Sweep::Config` the caller already supplied.

**The correction this decision needed.** A first draft claimed a unity path
reads "0 dB flat in band" and quoted a band of 28.3 Hz … 18665 Hz. Both were
wrong. The band figure mixed the fade seconds of one configuration with the
duration of another — `Sweep::Config`'s shipped defaults give 21.4 Hz … 19725.6 Hz
— and the flatness was never measured at all. Measured
(`tools/verify_l4a_pulse.py` section C):

| configuration | valid band | deviation across it |
|---|---|---|
| **defaults as shipped today**, T = 10 s (0.10/0.02 oct) | 21.4 – 19725.6 Hz | **−7.06 … +2.40 dB** |
| T = 2 s, 20 Hz – 20 kHz, fades 0.50/0.50 oct | 28.3 – 14142.1 Hz | −1.40 … +1.25 dB |
| T = 2 s, 20 Hz – 20 kHz, fades 2.00/0.50 oct | 80.0 – 14142.1 Hz | −0.01 … +0.03 dB |
| **defaults after decision 5**, T = 10 s (2.00/0.02 oct) | 80.0 – 19725.6 Hz | **−0.05 … +0.22 dB** |
| the C++ test fixture, T = 2 s, 100 Hz – 10 kHz (2.00/0.07 oct) | 400.0 – 9549.9 Hz | −0.03 … +0.14 dB |

The last two rows exist because a table without the configuration that ships,
and without the configuration the tests use, cannot ground a tolerance in either
of them. The fourth row was measured after a review pointed out that omission.

So the band formula defines where the answer is *meaningful*; it does not by
itself make the answer *flat*. Flatness is bought by decision 5's fade widths,
and at the widths this repo ships today the "0 dB" reading is worth ±7 dB at the
bottom of the band. Any claim about absolute level must quote the figure for the
configuration in use, and the implementation should expose the flatness it
achieved rather than let a caller assume it is zero.

**The rejected alternative and its exact defect.** Normalising by the *peak* of
`sweep ⊛ inv` is one line shorter and wrong at the third decimal: the peak
height depends on band-edge phase as well as in-band gain, so "0 dB" would be
approximately rather than derivably true, and no test could say by how much.

**What must not be attempted.** Dividing by the *spectrum* of `sweep ⊛ inv`
rather than by a scalar would flatten the response outside `[f1, f2]` too — and
would reintroduce division by near-zero, i.e. decision 1's regularisation
parameter through the back door. A scalar cannot do that.

## 5. The fade-in clamp is in the wrong units, and that costs 33 dB

**Decision.** `Sweep::Config` gains `fadeInOctaves` (default **2.0**), and the
clamp becomes

    fadeInSamples = round(fs · max(fadeInSec, 2/startHz, fadeInOctaves·ln2·L))

The existing `2/startHz` floor stays — it guards against a click at switch-on,
which is a different failure — but it stops being the only floor.

**What the research found.** The pre-arrival floor of `sweep ⊛ inv` (the
sidelobe level more than 50 ms before the peak, relative to the peak) is a
function of fade width measured in **octaves of sweep travel**, and is
independent of sweep duration in seconds. Measured with `tools/probe_sweep_fade.py`,
20 Hz–20 kHz at 48 kHz, at both T = 2 s and T = 10 s, agreeing within 1 dB:

| fade-in (octaves) | floor | | fade-out (octaves) | floor |
|---|---|---|---|---|
| 0.25 | −74.0 dB | | 0.25 | −109.6 dB |
| 0.50 | −75.4 dB | | 0.50 | −108.7 dB |
| 1.00 | −84.5 dB | | 1.00 | −106.9 dB |
| 1.50 | −98.0 dB | | 2.00 | −103.3 dB |
| 2.00 | **−108.7 dB** | | 3.00 | −99.9 dB |
| 3.00 | −122.9 dB | | | |

(T = 2 s column; the T = 10 s column agrees within 1 dB and is in the script's
output.) The two fades pull in **opposite directions**: a wider fade-in improves
the floor by roughly 19 dB per octave, a wider fade-out degrades it by roughly
3.5 dB per octave.

**Why they differ — half the hypothesis confirmed, half refuted.** This record
first guessed that the inverse filter's +6 dB/oct envelope was the whole story:
it sits at maximum exactly where the reversed fade-*out* lands and at minimum
where the fade-*in* lands, a weighting ratio of `f2/f1` = **60 dB** for a
20 Hz–20 kHz sweep. That is a testable prediction — strip the envelope and the
two fades should behave alike. Tested by rebuilding the inverse filter as plain
time reversal (`verify_l4a_pulse.py` section J):

| widened fade | 0.50 → 2.00 oct, envelope on | same, plain reversal |
|---|---|---|
| fade-in | −75.4 → −108.7 — **helps 33.3 dB** | −36.4 → −69.6 — **helps 33.2 dB** |
| fade-out | −75.4 → −70.0 — **hurts 5.4 dB** | −36.4 → −35.5 — neutral, 0.9 dB |

- **Confirmed:** the envelope is what makes a wide fade-out actively *harmful*.
  Without it, widening the fade-out is merely useless rather than damaging.
- **Refuted:** it does not explain the fade-in at all. Widening the fade-in buys
  the same 33 dB with the envelope and without it, so that benefit comes from
  softening the low-frequency spectral edge itself — a mechanism the envelope
  plays no part in.

**Two mechanisms, one of them now identified.** Stated as what was tested, which
is less than the original guess claimed and more than it had earned.

**Why the current clamp is a defect and not merely a default.** `2/startHz` is
a rule in *cycles at f1*, i.e. in seconds. The quantity that governs the floor
is octaves. For a 2 s sweep the clamp yields 0.5 octave; for a 10 s sweep the
same 0.1 s is 0.07 octave. So under today's code **lengthening the sweep makes
the band edges worse** — the opposite of what every operator will expect, and
invisible to the current test suite because no test looks at negative time. The
resulting −75 dB is below the >90 dB Müller states is needed for a
high-quality room impulse response.

**It is not numerical error.** Computed in float64 and float32 the floor is the
same to 0.1 dB. See decision 8.

**The obvious objection, tested and refuted.** A wide fade-in costs
low-frequency energy, so it is only worth paying if the artefact is *inside* the
band of interest. If the sidelobes lived below 20 Hz, band-limiting would remove
them for nothing and this decision would be over-engineering. That is the check
that would have killed it. Measured per octave band — filter-free, comparing the
pre-arrival segment against an equal-length segment centred on t = 0
(`verify_l4a_pulse.py` section A), as pre-arrival energy relative to the same
band of the main pulse:

| band | fade-in 0.5 oct | fade-in 2.0 oct |
|---|---|---|
| 10 – 20 Hz | −58.5 dB | −44.0 dB |
| **20 – 40 Hz** | **−71.5 dB** | **−88.9 dB** |
| 40 – 80 Hz | −78.5 dB | −105.8 dB |
| 80 – 160 Hz | −91.7 dB | −119.3 dB |
| 320 – 1280 Hz | −123.8 dB | −150.1 dB |
| 5120 – 20480 Hz | −189.1 dB | −200.3 dB |

The artefact is worst at **20–40 Hz — inside the band of interest, not below
it.** Band-limiting cannot remove it. The objection fails and the decision
stands. (Below 20 Hz the wide fade reads *worse* because it deliberately
suppresses the sweep's own energy there; that band is outside the measurement
and the ratio is not meaningful in it.)

**And the practice that looked like a substitute is not one.** It is common
advice to start the sweep below the band of interest — 10 Hz rather than 20 Hz —
so the useful band carries full energy. Compared at constant sweep rate so only
one thing changes (section B), as in-band pre-arrival energy over 20 Hz – 20 kHz:

| | floor |
|---|---|
| f1 = 20 Hz, fade 0.5 oct — today's default | −99.9 dB |
| f1 = 10 Hz, fade 0.5 oct — the "start lower" practice | −91.0 dB |
| f1 = 5 Hz, fade 0.5 oct | −93.9 dB |
| f1 = 20 Hz, fade 2.0 oct | **−132.0 dB** |
| f1 = 5 Hz, fade 2.0 oct | −120.8 dB |

Lowering `startHz` alone made the artefact **worse**, not better. There is no
contradiction: the reason for starting low is to give the useful band full
energy — a **signal-to-noise** measure, addressing a different problem than
pre-arrival artefacts. Two knobs, two problems. Reading one as a substitute for
the other is the mistake this measurement prevents.

> **A citation this record got wrong, left visible on purpose.** The first draft
> attributed the "start lower" advice, and decision 9's capture rule, to
> **AES-2id**. AES-2id is *Guidelines for the use of the AES3 interface* — a
> document about a two-channel digital audio **cable format**. It says nothing
> about excitation signals. The attribution came from a third-party web page
> that presented itself as an AES-2id guide, and it was carried into this record
> without anyone checking the standard's actual scope. Every measurement in the
> two affected sections stands; only the provenance was fiction. **Fetching a
> page is not reading a standard**, and a citation nobody verified is worse than
> no citation, because it borrows authority it has not got.

**The trade-off is the caller's to make, so it is exposed.** A wide fade-in
tapers the bottom of the sweep: at 2 octaves from 20 Hz, everything from 20 to
80 Hz is amplitude-shaped, costing signal-to-noise exactly where room modes
live. Three legitimate positions exist and `fadeInOctaves` plus `startHz`
select between them:

- **Both, at the cost of a longer sweep** — `startHz` two octaves below the band
  of interest (5 Hz) *and* `fadeInOctaves = 2`. The fade then covers 5–20 Hz, so
  the whole band of interest is at full amplitude and the floor is −120.8 dB.
  This is the configuration to recommend to an operator measuring a room.
- **Dynamic range first** (the default, `fadeInOctaves = 2` from 20 Hz):
  −132.0 dB, with 20–80 Hz tapered.
- **Low-frequency energy first** (`fadeInOctaves = 0.5`): −99.9 dB in band, full
  amplitude from 28 Hz. Correct for a quick check.

### The blocker this decision walked into: `buildInverseFilter` fades twice

**Everything above was measured on a model of the inverse filter that the
shipped C++ does not implement**, and applying decision 5 to the C++ as written
would make the measurement catastrophically worse rather than better. Found by a
reviewing session reading `core/src/gen/Sweep.cpp:151-162`; verified here.

`buildInverseFilter()` builds `inv` from the **already-faded** sweep, reversed
and shaped — so it inherits a taper at both ends, with the forward sweep's
fade-*out* landing at `inv`'s **start** and its fade-*in* at `inv`'s **end**.
It then applies a **second, direct Tukey layer**: `fadeInSamples_` at the start
of `inv` and `fadeOutSamples_` at the end. Since `inv[0]` corresponds to the
sweep's last sample, that second fade-in tapers the kernel's **highest
frequencies** — the direction this decision's own table calls harmful.

Today the two layers coincide in size and nobody notices. With a two-octave
fade-in they do not. Flatness across the valid band, measured both ways:

| configuration | fade-in | C++ as shipped | inherited taper only |
|---|---|---|---|
| golden `sweep_deconv` | `2/f1` (today) | 11.12 dB | 10.65 dB |
| golden `sweep_deconv` | **2 octaves** | **51.39 dB** | **0.17 dB** |
| `Sweep::Config` defaults | `2/f1` (today) | **22.94 dB** | 9.19 dB |
| `Sweep::Config` defaults | **2 octaves** | **72.36 dB** | **0.27 dB** |

Two things follow. The second layer is **already costing 13.75 dB of in-band
flatness at the default configuration today**, invisibly, because no test looks
at the band. And decision 5 cannot land until it is removed: it would turn a
72 dB improvement into a 72 dB regression.

**Its stated justification is false in the ordinary case.** The comment says
"without this the inverse filter starts/ends with a step". It does not:
`forward[]` is multiplied by `fadeEnvelope`, which is zero at both ends, and the
reversal carries those zeros. The layer multiplies something that is already
zero. It is only load-bearing when `fadeOutSec == 0`, where the *forward* sweep
ends with a step — and the fix for that belongs on the forward sweep, as a
minimum fade-out clamp of two cycles at `endHz` (5 samples at 20 kHz), matching
the `2/startHz` floor already on the fade-in.

**No test caught any of this**, because the two layers are the same size in
every fixture, which makes the second one invisible. Trap #3 in the
"green but proving nothing" table, found again.

**Consequence for existing tests.** `test_generator_sweep.cpp` asserts
`fadeInSamples()` equals the `2/startHz` clamp. Whether that assertion still
holds depends on which of the three floors dominates for its fixture; the
implementation plan must re-derive it rather than adjust it until green.

## 6. Polarity: peak-relative first arrival, with a refusable confidence

**Decision.** `Polarity` returns a sign, the index it decided on, and a
confidence. It reports `Unknown` rather than guessing when confidence is low.

The first arrival is the first index at or after `originIndex` whose magnitude
reaches `arrivalFraction` (default 0.5, i.e. −6 dB) of the largest magnitude in
the search window. The sign is that sample's sign.

**Why the threshold is a Config field and not a constant.** It is a convention,
not a derivation — the patents surveyed variously use "first peak above a
threshold" and "sign of the maximum after band-pass and exponential weighting".
A convention hard-coded is a convention nobody can check; exposed, a test can
drive it.

**Why the noise estimate has a derivable window.** Confidence is peak magnitude
over the RMS of the region between `originIndex − L·ln 2` and `originIndex`.
That window's left edge is derived rather than chosen: by decision 3's formula
H2 is the first harmonic packet and it sits at exactly `−L·ln 2`.

**Corrected 2026-08-30.** This paragraph used to end "so it contains noise and
sidelobes only", and that is false on any system with distortion. The window's
left edge sits *on* the H2 packet, whose peak is measured about 2.44 samples
late (`core/tests/test_ir_deconvolver.cpp`), so H2 energy lands inside the
window and its causal tail runs toward the origin. The denominator is therefore
the **pre-arrival floor**: noise, sidelobes, and residual harmonic energy. That
is the right denominator operationally — a distorting system should read as less
trustworthy — but the window must be described for what it measures. The edge
stays where the formula puts it; only the claim about its contents was wrong.
Moving it to sit between H2 and H3 would be worse: `−L·ln 2.5 ≈ −0.916 L` lies
between H3 and H2, so a window starting there would *contain* H2 entirely.

**Why the answer is meaningful at all.** The analysis pulse was measured
symmetric about its peak to 3.0e−5 and peaks **positive** (decision 3), so it is
zero-phase to within that error, and the sign of the arrival is the sign of the
system. A pulse that was not zero-phase would make polarity a matter of opinion.

**The failure it was tested against.** The rule "first sample reaching a
fraction of the peak" can in principle latch onto a *precursor* sidelobe of the
opposite sign, reporting the polarity backwards — the worse failure, because a
confident wrong answer sends someone to rewire a working loudspeaker. Driven
through three systems of increasing hostility at four thresholds each, both
polarities (`verify_l4a_measurement.py` section E):

| system | first arrival, fractions 0.2 → 0.7 | sign |
|---|---|---|
| full range, 60 Hz – 15 kHz | +0 … +1 samples | correct at all four |
| horn, 500 Hz – 8 kHz | +1 … +2 samples | correct at all four |
| subwoofer, 30 – 120 Hz | +67 … +158 samples | correct at all four |

The subwoofer is the case that should break it — a 90 Hz-wide passband rings for
tens of milliseconds and has large excursions of both signs — and the sign
survives. Note how far the *index* moves with the threshold there (67 to 158
samples, 1.4 to 3.3 ms): the reported arrival time from a narrowband system is
threshold-dependent and must not be quoted as a delay. The **sign** is what this
class promises.

**The non-minimum-phase case, which this record first left untested.** Five more
systems, both polarities each (`verify_l4a_measurement.py` section H). "Symmetry" is
the largest positive excursion over the largest negative one in the arrival
window — near 0 dB means the waveform has no dominant sign at all:

| system | sign read | confidence | symmetry |
|---|---|---|---|
| minimum phase `[1, 0.5]` | correct | 66.9 dB | 9.0 dB |
| maximum phase `[0.5, 1]` | correct | 71.1 dB | 10.3 dB |
| 2nd-order allpass | correct | 61.5 dB | 2.1 dB |
| inverted reflection, 1.5× the direct, 3 ms later | correct | 67.7 dB | 3.5 dB |
| **narrowband, 200 – 250 Hz** | **WRONG** | **129.7 dB** | **0.1 dB** |

Two things follow, and the second is a defect in this decision as first written.

1. Non-minimum phase alone does **not** break it. Maximum phase, a true allpass,
   and an inverted reflection louder than the direct sound all read correctly —
   the 50% threshold latches onto the direct arrival before the reflection lands.
   **Corrected 2026-08-30: that last clause is false past filter order 8.** At
   order 8 the skirt's group-delay ripple makes the second excursion larger than
   the first, so the first arrival never reaches 50% of the window peak and the
   rule steps *over* it onto a later, opposite-signed lobe. Measured on
   `butter(8)` spanning 1000–16000 Hz: at `arrivalFraction` 0.2 and 0.3 the rule
   reads index 1, sign correct; at 0.5 and 0.7 it reads index 4, sign wrong. The
   same failure appears with an inverted reflection present. The threshold does
   not latch onto the direct arrival — it latches onto the first excursion large
   enough, and at high order that is not the same thing.
2. **The confidence figure does not detect the one failure. It is the highest
   reading in the table.** Confidence as decision 6 defines it measures
   signal-to-noise, and a narrowband system has a superb signal-to-noise ratio
   and no definable polarity. Answering "is this loud enough to trust?" is not
   answering "does this waveform have a sign?"

**A symmetry gate was proposed here, and the survey killed it.** The draft
concluded that the class should return `Unknown` when symmetry falls below
1.0 dB, on the strength of those five systems. A review objected that five is
not a survey, so the surface was measured properly: bandwidth from 0.5 to 8
octaves at four centre frequencies, both polarities, 28 cells
(`verify_l4a_measurement.py` section L). Symmetry in dB; **X** marks a
first-arrival sign that disagrees with the drive polarity:

| centre | 0.5 oct | 1 oct | 2 oct | 3 oct | 4 oct | 6 oct | 8 oct |
|---|---|---|---|---|---|---|---|
| 60 Hz | 0.35 **X** | 0.39 **X** | 3.03 | 0.85 | 4.14 | 9.21 | 12.16 |
| 250 Hz | 0.26 **X** | 0.45 **X** | 4.18 **X** | 1.20 | 3.41 | 10.38 | 12.71 |
| 1000 Hz | 0.27 **X** | 0.46 **X** | 4.14 **X** | 1.33 | 2.93 | 8.76 | 9.20 |
| 4000 Hz | 0.41 **X** | 0.54 **X** | 3.46 **X** | 4.07 | 0.09 | 6.34 | 9.29 |

**Symmetry does not separate the two populations.** Agreeing cells run from
0.09 dB to 12.71 dB; disagreeing cells run from 0.26 dB to 4.18 dB. The
distributions overlap almost entirely, and the decisive pair sits at opposite
ends of the gate: 4 octaves at 4 kHz reads **0.09 dB and agrees**, while
2 octaves at 250 Hz reads **4.18 dB and disagrees**. A threshold at 1.0 dB would
refuse the first and pass the second. **No threshold on this figure works**, and
the proposal is withdrawn rather than tuned.

> ### SUPERSEDED 2026-08-30 by the step-0 survey — read decision 6b instead
>
> Everything from here to the end of decision 6 was derived from a survey of
> **one filter family at one order** (`scipy.signal.butter(4, ...)`). Widening
> the survey destroyed the conclusion: `butter(8)` spanning 1000–16000 Hz
> measures **4.18 octaves** and still answers backwards. The 2.5-octave gate
> does not exist as a boundary; it was the floor of that grid.
>
> The text is kept rather than deleted. The reasoning below is sound given what
> it had, and a wrong conclusion with its derivation visible teaches more than a
> tidy file that hides how it was reached. **Do not implement from this
> section.** Decision 6b replaces it.

**What the survey does show, and it is a better answer.** From **2.5 octaves of
bandwidth upward, every surveyed cell agrees**; at 2 octaves it agrees at four of
five bass centres and only one of four wideband ones; below that it is scattered.
Bandwidth is the variable that matters, and the reason it matters is not a defect
in the code:

> **For a band-limited system, the sign of the first arrival is a property of
> that system's own phase response, not of how it is wired.** Asking "is this
> subwoofer connected backwards?" is not a question a measurement of the
> subwoofer alone can answer.

What *is* always answerable is **relative** polarity. Negating the drive negates
the recovered response exactly — that is linearity — so comparing one box
against another, or the same box before and after a rewire, is sound at any
bandwidth. The absolute verdict is the one that needs the bandwidth.

**So the class returns a bandwidth figure and refuses below it.** The −10 dB
bandwidth of the recovered response, in octaves, computed from its own spectrum;
`Unknown` below a configurable minimum, default **2.5 octaves**.

**Why 2.5 and not 3.** A first pass jumped from 2 to 3 octaves and put the
boundary at 3. Filling in 2.5, and adding a finer grid across the bass where a
polarity checker actually earns its keep, moved it:

| bandwidth | centres 60 / 250 / 1k / 4k Hz | centres 30 / 45 / 60 / 90 / 120 Hz |
|---|---|---|
| 2.0 oct | 1 of 4 agree | 4 of 5 agree |
| **2.5 oct** | **4 of 4** | **5 of 5** |
| 3.0 oct | 4 of 4 | 5 of 5 |

2.5 is the **lowest measured width that is clean everywhere**, so it admits the
most while admitting no known failure. It is still **a boundary observed, not
derived**: nine centre frequencies, one filter family, one filter order. The
surface below it is not monotonic — a 30 Hz centre agrees at 1.0 octave, then
disagrees at 1.5 and 2.0 — which is itself the signal that what governs it is
phase detail rather than a smooth law. Widen the survey before trusting it.

**What is still untested.** A multi-way loudspeaker measured off-axis, where the
drivers' arrivals separate in time; anything measured through a system with
significant excess phase from a real crossover. Untested, not proven safe.

## 6b. Polarity, second pass: the gate is a pair of band edges

*2026-08-30, after the step-0 survey that decision 6's own owner condition
demanded. Three independent measurers — the building session, the reviewing
session, and an adversarial reviewer — each built their own grid and their own
NumPy model. Where a number below is attributed to one of them, the other two
reproduced it. Scripts: `tools/probe_polarity_bandwidth.py`,
`tools/probe_polarity_margin.py`, `tools/probe_polarity_edges.py`.*

**Decision.** `findPolarity` answers only when the recovered response spans both
ends of the audio band: **measured low edge ≤ 100 Hz AND measured high edge ≥
8 kHz**, both read from the arrival's own spectrum at −10 dB. Outside that box it
returns `Unknown` with a directional reason. `arrivalFraction` stays at **0.5**.
The gate is stateless and carries no widening constant.

### Four gate variables were measured. Three of them died the same death

| variable | why it failed |
|---|---|
| bandwidth in octaves (decision 6) | no constant is safe; the widest disagreeing measurement climbs with filter order — 0.85 oct at order 2, 2.50 at order 4, 4.18 at order 8 — with no ceiling on any grid |
| arrival ambiguity margin | on 256 full-range boxes that all answer **correctly**, only 26% have margin ≤ 0.30 (12% on the reviewing session's grid), so a zero-lie margin gate refuses 74–88% of good boxes. Its apparent climb with filter order — floor 0.708 at order 2 down to 0.300 at order 16 — was measured under the **peak-sign** rule and does not transfer to the shipped rule; see the attribution note below. Under the shipped rule the band-edge gate needs no margin gate at all |
| low band edge alone | wrong answers reach down to 23.4 Hz, the same floor as correct ones |
| **both band edges together** | **holds** — see the envelope below |

The three that died share a shape, and naming it is the most portable thing this
lane produced:

> **A scalar gate calibrated on a finite grid of filters will be beaten by a
> steeper filter, unless the gating variable carries a bound from outside the
> grid.** 2.5 octaves, 0.5 margin and 0.537 margin were each read off a
> different grid and each was overturned by the next one. Before adopting a
> threshold, ask what stops the surveyed quantity from moving — not what value
> the survey produced.

### Why two edges and not one width

An octave width conflates two different things. `butter(8)` spanning
1000–16000 Hz is four octaves wide and answers backwards; `butter(4)` spanning
60–960 Hz is also four octaves and answers correctly. The first is a horn. What
separates them is not how wide the band is but **where it sits**, and the
physical statement behind that is available before the data:

> The sign of a first arrival needs a low end to give the wavefront a direction
> and a high end to give it a front. A system missing either does not have an
> arrival whose sign is a property of its wiring.

That is a claim that can be wrong, and it was tested at the corner rather than
assumed. It is not a bound in the sense the lesson above demands — it does not
stop the boundary moving the way Cauchy–Schwarz bounds a correlation — but it is
a statement about loudspeakers rather than a number read off a grid.

### The envelope, stated so it can be reproduced and disbelieved

Zero wrong answers were measured **for single-section systems**, under all of:

- the **shipped verdict rule** — first arrival reaching 0.5 of the window peak.
  This qualifier is load-bearing; see the attribution note below.
- families `butter`, `cheby1`, `ellip`, `bessel` at **orders 2 through 16** —
  the whole range surveyed, not a subset;
- linear-phase FIR, brickwall, 4097 taps;
- minimum-phase FIR of the same length — **one construction only**
  (`firwin` + Hilbert), not a swept family;
- band edges read as **measured** −10 dB crossings of the arrival spectrum, not
  design edges;
- the estimator of the next section, with all three of its defects fixed;
- both drive polarities, on two independently built grids.

**About the window-agreement check, and why it is not in that list as a filter.**
Every measurement was also re-measured with a fixed 0.5 s window and the two
readings compared. With the estimator fixed, that check **rejects nothing**: over
54 valid cells the two windows disagree by at most 0.033 octave, and the
previously-degenerate cell — `cheby1` order 12 at 50–71 Hz, the one that used to
report an 18 kHz edge — now disagrees by 0.025 octave, *less* than the worst
valid cell. There is no separation because there is nothing left to separate; the
adaptive window already removed the fiction. So the check is reported as a check
that passed, not as a screen that shaped the result. An earlier round of this
lane did use such a screen, with a threshold chosen from nowhere, and it silently
discarded 48 counterexamples while printing "zero failures" — a screen that
removes the evidence against a hypothesis proves the hypothesis and nothing else.

**The attribution that nearly went into this record wrong.** An earlier draft of
this section published an envelope of "orders ≤ 8" and a table of cliffs at
`cheby1` order 9 and `butter` order 16. Every one of those failures was counted
with the **peak-sign** rule — the sign of the window's largest excursion — which
is not the rule that ships. Re-counted under the shipped rule on the same 124
admitted measurements:

| sign rule | wrong answers |
|---|---|
| first arrival at 0.2 of peak | 0 |
| **first arrival at 0.5 of peak (ships)** | **0** |
| sign of the window peak | 26 |

The 26 are `butter` order 16 and `cheby1` orders 9, 12 and 16. They are a
property of a rule this project does not use, and attributing them to a filter
family or order would have made the envelope narrower than the truth — the same
mistake decision 6 made when it read a property of `butter(4)` as a property of
bandwidth. Both sessions had counted it the same wrong way; the reviewing session
caught it.

**Where those 26 go instead** — they are not discarded. Under the shipped rule
they are measurements whose window peak has the opposite sign to the verdict,
which makes `margin` exceed 1. That is the display doing its job: a margin above
1 says "the largest thing in this window disagrees with the answer", which is
exactly when an operator should look closer. `margin` is therefore defined
relative to the **verdict's** sign and is deliberately allowed to exceed 1.

### `arrivalFraction` is 0.5, and the reason must survive the next session

An earlier pass in this lane found 0.2 gave fewer wrong answers across an IIR
grid and nearly adopted it. **Linear-phase FIR refutes it**: its symmetric
pre-ring puts an opposite-signed lobe above the 20% threshold before the main
lobe arrives, so `arrivalFraction = 0.2` answers backwards on both drive
polarities. `0.5` and the peak-sign rule are both clean there. Linear-phase FIR
presets are ordinary in modern line-array processing, so this is not an exotic
case. A future session reading only the IIR table will see 0.2 winning and
change it back in good faith — that is trap #17, and this paragraph exists to
stop it.

### The estimator is a precondition, not a tidy-up

Three defects were measured in the −10 dB edge estimator decision 6 assumed, and
the gate cannot be read off it until all three are fixed
(`tools/probe_polarity_edges.py`):

1. **A fixed 50 ms window cannot resolve a low band.** A `cheby1` order 12
   band-pass designed 50–71 Hz reported edges of **46.9 Hz – 18270 Hz** while the
   driven signal is −101 dB at 10–20 kHz. That reading placed a subwoofer inside
   a gate built to refuse it. Fixed: size the window at ten cycles of the low
   edge just measured, then measure again. The same cell then reads
   **51.5 – 72 Hz**.

   **And a condition on the fix that the fix itself did not state.** The window
   can only be resized if the capture is long enough to hold it. Ten cycles of a
   47 Hz low edge is 0.21 s, while decision 9 sizes the capture gap at 1.5x
   RT60 -- so a dry room at RT60 = 0.1 s leaves 0.15 s and the window cannot be
   built. Both the C++ and the NumPy transcription originally fell back to the
   first-pass 50 ms reading in that case, which is to say **both quietly handed
   back the very fiction this defect exists to remove**: measured, the same
   subwoofer cut to a 0.15 s capture returns 40.7 Hz - 18272 Hz, and the gate
   admits it. Neither implementation was drifting from the other; both had
   failed to ask when the re-measurement does not happen. They now refuse --
   `Refusal::CaptureTooShort` in C++, `None` in the probe. A silent fallback is
   how a fixed defect comes back.

   **Scope of that rule, stated before anyone meets it.** Keying the window
   length to the low edge assumes the response's ringing is dominated by its
   band edges, which is what every system in this survey was — band-pass
   filters, ringing at their skirts. A high-Q resonance in the *middle* of the
   band would break the assumption: Q ≈ 50 at 200 Hz rings for roughly 0.25 s,
   where ten cycles of a 100 Hz low edge is 0.1 s, so the window would truncate
   a decay the rule has no way to see. Real loudspeakers rarely reach a Q of
   more than a few, which is why this is a stated limit and not a code change.
   Unsurveyed, not proven safe.
2. **Bins that were never excited are counted.** Below the sweep's start
   frequency there is leakage and inverse-filter fade, not response. Fixed: clamp
   the search to the excited band, which means `Deconvolution` must carry the
   band edges rather than treating them as the caller's business.
3. **Integer bin indices quantise the answer** — 0.16 octave per bin step near
   100 Hz. Fixed: interpolate the crossing between adjacent bins in log
   magnitude.

Fixing these removes fiction; it does **not** reduce spread. Measured before and
after, the inter-family spread of the low edge for systems designed at 100 Hz was
0.737 and then 0.905 octave. The reviewing session predicted the fix would shrink
it and was wrong; the fix is still mandatory, for defect 1 alone.

### No hysteresis, and why the obvious worry was a mislabelled number

Both sessions spent a round believing a 0.9-octave scatter would make a box on
the threshold flicker between `Answer` and `Unknown`. That number varies filter
family, order **and** noise at once — twelve different loudspeakers, not one
measured twice. The flicker question needs one system measured repeatedly:

| system | spread over 20 measurements at 10 dB SNR |
|---|---|
| `bessel` order 4 | 0.057 octave |
| `butter` order 4 | 0.032 octave |
| `butter` order 8 | 0.013 octave |
| `ellip` order 8 | 0.005 octave |

Nothing crosses the threshold, even at an SNR no one would accept. **So the core
gate is stateless and exact, and no widening constant enters `core/`.** The
ordering matches the observed skirt steepness, so the residual uncertainty of an
edge is a function of local spectral slope — worth exporting one day as a
resolution figure, not worth a constant today.

**The residual, stated rather than left to be discovered:** a Bessel-processed
box sitting on the gate is refused **from both edges at once**, because a gentle
skirt pulls the measured low edge up *and* the measured high edge down. Measured:
`bessel` order 8 designed 100–8000 Hz reads **110.2 – 7378 Hz** on the reviewing
session's grid, failing both halves of the box; orders 6 and 8 designed at 100 Hz
read 100.6 and 110.6 Hz on this one. These are legitimate loudspeakers and the
refusal is a false one.

It is, however, a *consistent* false refusal — 0.006 octave of repeat spread, so
the same box gives the same answer every time — and the refusal reason carries
the numbers, so an operator sees "low edge 110.6 Hz, gate 100 Hz" rather than a
verdict that changes when they measure again. A wrong answer that never changes
and shows its working is a different object from one that flickers.

### The limit no polarity checker escapes, including every competitor

A two-way box with a 3rd-order Butterworth crossover and a **tweeter inverted by
design** — a convention in commercial loudspeakers, and mandatory for LR2 and BW2
— sits inside the box, is a genuine full-range system with a healthy estimate,
and reads negative. The tool is not wrong: the first arriving wavefront really is
the tweeter and it really is inverted. Only the interpretation "this box is wired
backwards" is wrong, and the information needed to reject it — the designer's
intent — is not in the signal.

This is the same category error as asking whether a subwoofer is in polarity with
a main, seen from inside one cabinet instead of across a crossover. **Any checker
that has only the measurement to read has it** -- the designer's intent is not in
the signal, so no estimator recovers it. A tool carrying per-model design
documentation could answer, because that is a second source of information rather
than a better algorithm; none of the surveyed analysers carries one, and they
ship the verdict ungated and let the operator judge.

The qualifier matters: an earlier draft of this paragraph said "every absolute
polarity checker", which overstates a measured observation into a claim about
every tool that could ever exist, and quietly hides the one real way out. What this project can do that
they do not is say so, and show the ambiguity margin: the **design-inverted**
two-way case reads 0.90–0.98, and the non-inverted two-way control reads
0.85–0.91, where a clean single-section full-range box reads a median of 0.46.
Note that the margin flags the whole two-way *class* as ambiguous, not the
inverted wiring specifically — which is the honest thing for it to do, because
the measurement cannot tell those apart either.

**So `margin` ships as a displayed number, not as a gate.** It failed as a gate
on measurement, and it is genuinely informative exactly where the gate cannot
help.

### Owner decisions recorded

The G21 approval of 2026-08-30 reached the building session second-hand, through
a peer session. That session did not build on it; it recorded the relay and
asked the owner to confirm in its own conversation before writing a line. Every
decision below was taken in the building session's own conversation, and the
gate variable changed **twice** after the first of them — the trail matters more
than the endpoint, because a later session reading only the endpoint would not
know which parts were measured into place and which were chosen.

- **B1, approved 2026-08-30 in the building session.** Ship absolute polarity
  with a gate and state the envelope honestly, rather than withholding the
  feature. Weighed against every surveyed competitor shipping it with no gate at
  all, so any gate is a strict improvement on the state of the art
  (`docs/dsp/2026-08-28-competitive-parity.md` G21 is an owner floor directive).

  **B1 amendment 1 — the gate variable, margin → band-edge box.** B1 was
  approved with the *arrival ambiguity margin* as the gating variable, on
  numbers available at the time. Measured afterwards: a zero-lie margin gate
  refuses 74–88% of full-range boxes that all answer correctly. The building
  session brought that back to the owner rather than substituting quietly, the
  reviewing session was asked to check it, and the box replaced the margin.

  **B1 amendment 2 — the envelope, "orders ≤ 8" → orders 2–16.** The first
  envelope was written from failures counted under the peak-sign rule, which
  this project does not ship. Re-counted under the shipped rule the failures
  vanish, so the published envelope widened rather than narrowed. This amendment
  needed no owner decision — it corrected an attribution error, it did not
  change what ships — but it is recorded here because the two amendments look
  identical from a git log and are not: the first changed the design, the second
  corrected a measurement.
- **G21 tier 3 amended, approved 2026-08-30 in the building session.** The
  guided "measure the main, then measure the sub against it" workflow moves to
  the alignment wizard G17 as a **phase** question. Measured: a correctly wired
  sub/main pair reads the **opposite** sign at some crossover orders and the
  correct sign at others — which orders depends on the filter convention, and
  the two sessions measuring it got different sets. **The order-dependence is
  itself the finding**; do not read any particular order as safe. The reason it
  cannot be fixed by a better estimator: the correct phase offset at a crossover
  is set by the intended topology — 0° for Linkwitz-Riley, 180° for 2nd-order
  Butterworth, 90° for odd-order Butterworth — and no measurement recovers the
  designer's intent.

## 7. Frequency response: start **before** t = 0, by two cycles of `f_lo`

**Decision.** `IrSpectrum` transforms from `originIndex − round(2·fs/f_lo)` to
the end of the buffer, and returns complex bins. Phase stays in **radians** in
`core/`.

**Why the window does not start at t = 0, which is what this decision first
said.** The analysis pulse is symmetric about its peak (decision 3 measures the
symmetry at 3.0e−5). So the deconvolved signal `h ⊛ p` has real content on
*both* sides of `originIndex`, and a transform beginning exactly there slices
the pulse down the middle. That is not a subtle error. Recovering two known
filters and comparing against their analytic responses over 100 Hz – 10 kHz
(`verify_l4a_measurement.py` section D):

| lead-in kept | 2nd-order 1 kHz lowpass | 4th-order 300 Hz highpass |
|---|---|---|
| **0 ms (the naive window)** | +1.33 … +2.55 dB | **−0.09 … +23.48 dB** |
| 5 ms | −1.23 … +1.05 dB | −0.02 … 0.00 dB |
| 20 ms | −0.02 … +0.02 dB | 0.00 dB |
| 50 ms | 0.00 dB | 0.00 dB |

A 23 dB error on a highpass, from a windowing choice, on a path whose pure-delay
round trip was exact to the sample and to four decimal places of gain. The delay
test passed because it only inspected the peak — **a test that looks only at the
peak cannot see a window that damages the skirts.**

**Why two cycles rather than 20 ms.** Milliseconds would be a constant somebody
picked. The pulse's width is set by the bottom of the valid band, so the lead-in
is expressed in cycles of `f_lo` and read off the same Config as everything
else. Measured against the 300 Hz highpass at two fade widths, worst-case error
across the band:

| `f_lo` | 1 cycle | 2 cycles | 4 cycles | 8 cycles |
|---|---|---|---|---|
| 28.3 Hz | 0.00 dB | 0.00 dB | 0.00 dB | 0.00 dB |
| 80.0 Hz | 0.01 dB | 0.00 dB | 0.00 dB | 0.00 dB |

Two cycles is one doubling past where the error vanishes at both widths. It also
echoes the `2/startHz` fade floor already in `Sweep` — the same "two cycles of
the lowest frequency that matters" reasoning, which is not a coincidence: both
are asking how long the slowest thing in the signal takes.

**What the caller must be told.** The returned spectrum's time reference is the
start of that window, not `t = 0`, so it carries a linear phase term of
`2·fs/f_lo` samples. Either `IrSpectrum` removes it or it reports it. Leaving it
implicit is how a group-delay readout acquires a constant offset nobody can
find.

**Why no user-adjustable window.** A window over the IR is a gate, and the gate
is L4c's draggable one (G25). Shipping a fixed *gate* here would mean L4c must
undo a decision this lane had no reason to make. The lead-in above is not a gate
— it is the minimum window that does not corrupt the answer, and there is no
setting of it that a user would want smaller.

**Why radians.** `app/` converts to degrees exactly once, in `Analyser::pushPair`
(see `docs/dsp/2026-08-29-display-layer-l5c.md`). A second conversion point in
`core/` is how a factor of 57.3 gets applied twice.

## 8. float32 is not the limiting error, and this is why we checked

**Decision.** The existing `RealFft` (single precision) is used. No
double-precision FFT is written for the offline path.

**Why, with the number.** The pre-arrival floor computed **natively** in float32
and in float64 agrees to 0.0 dB at 0.5, 2.0 and 3.0 octaves of fade-in —
including at −122.9 dB. So single precision is not the limit anywhere in the
range decision 5 makes reachable, and writing a double-precision transform would
improve a term that is not binding. The repo's existing note that `RealFft` is
single precision (`memory/float32-fft-precision.md`) is why this was checked
rather than assumed.

**How this claim was wrong the first time.** The original measurement computed
the whole convolution in double precision and then cast the *result* to float32.
That measures double precision and rounds the answer; it says nothing about
single-precision arithmetic, and the agreement it "found" was guaranteed by
construction. `numpy.fft` silently promotes float32 input to float64, which is
what made the mistake invisible. The corrected measurement uses `scipy.fft`,
which honours the input dtype, and `tools/l4a_sweep.py` asserts the
intermediate spectrum is `complex64` before believing the comparison — because a
precision test that has silently stopped testing precision still prints a
reassuring number.

**Where it becomes the limit: nowhere reachable.** An earlier version of this
decision refused to guess and left the crossover unmeasured. It has now been
looked for along both axes that could move it — fade width, which lowers the
sidelobe floor, and transform size, which raises the arithmetic noise
(`verify_l4a_pulse.py` section I):

| fade-in | float64 | float32 | | sweep length | transform | difference |
|---|---|---|---|---|---|---|
| 2 oct | −108.7 | −108.7 | | 2 s | 2¹⁸ | 0.0 dB |
| 4 oct | −133.9 | −133.9 | | 8 s | 2²⁰ | 0.0 dB |
| 6 oct | −147.7 | −147.7 | | 32 s | 2²² | 0.0 dB |

Agreement to 0.1 dB down to **−147.7 dB** and up to a **2²² transform** — beyond
anything this lane can produce, since a 6-octave fade-in on a ten-octave sweep
tapers most of the signal away. Single precision is not the limit anywhere in
the reachable space, and the "must re-measure past 3 octaves" caution the
earlier draft carried can be retired.

Worth noting for decision 5's benefit: the floor **saturates near −134 dB at
4 octaves** and buys little after that, at any sweep length. The fade law is not
a straight line forever.

**One consequence for the implementation.** The FFT is sized
`nextPow2(Ny + Ninv − 1)`, which for a 10 s sweep and a 12 s capture at 48 kHz
is 2²¹ = 2 097 152. `RealFft` builds tables of N/2 complex entries, so that one
transform costs about 8 MB of tables plus the working buffers. Acceptable
offline, and a reason this path must never be constructed on the audio thread.

## 9. The capture must outlast the sweep by 1.5 × the reverberation time

**Decision.** `Deconvolver` takes the captured buffer as given, but the record
states the rule the caller must satisfy, and the class exposes enough for the
caller to check it: the recording must continue past the end of the sweep by at
least **1.5 × the expected RT60**.

**Where the rule comes from, corrected.** A first draft attributed it to
AES-2id, which is a document about the AES3 digital interface and says nothing
about excitation signals — see the boxed note under decision 5. The actual
sources are two:

- **Müller & Massarani (JAES 49(6), 2001)**, which states it directly: the
  data-acquiring period must be long enough to capture all delayed components,
  so the sweep must be somewhat shorter than the capturing period and the FFT
  length that follows it. Qualitative, and correct.
- **ISO 18233:2006**, *Application of new measurement methods in building and
  room acoustics*, which is the standard that governs deterministic-excitation
  impulse-response measurement — including the swept sine — as a substitute for
  the classical ISO 3382 procedures. **Not purchased**, so nothing here quotes a
  clause from it, and no claim in this record rests on one. It is named so a
  later session knows which document to buy rather than which web page to trust.

The measurement below is this project's own, and it is *sharper* than the
qualitative rule: it says how much longer. Measured against a synthetic room
with an RT60 of exactly 1.0 s — band-limited exponentially decaying noise plus a
clear direct arrival — recovering its frequency response over 100 Hz – 10 kHz
(`verify_l4a_measurement.py` section K):

| gap after the sweep | gap / RT60 | rms error | worst error |
|---|---|---|---|
| 0.00 s | 0.00 | 1.46 dB | **32.55 dB** |
| 0.25 s | 0.25 | 0.34 dB | 21.90 dB |
| 0.50 s | 0.50 | 0.07 dB | 4.61 dB |
| 1.00 s | 1.00 | 0.00 dB | 0.60 dB |
| **1.50 s** | **1.50** | 0.00 dB | **0.02 dB** |
| 2.00 s | 2.00 | 0.00 dB | 0.01 dB |

Note how badly **rms error misleads** here: at half the RT60 the rms is already 0.07 dB — looks finished —
while the worst bin is still out by 4.61 dB. An average taken over a spectrum
hides exactly the narrow-band errors a truncated decay produces. One doubling
past `gap = RT60` is where the worst case falls under 0.1 dB, and that is the
number to publish.

**What the operator sees if they ignore it.** Truncating the decay is a
rectangular window on the tail, so the error is a rippling comb, worst at low
frequencies where the decay is longest — indistinguishable, on screen, from
room modes. It will be read as a room problem and someone will try to EQ it.

## 10. How CI proves this with no sound card

Eight assertions, none of which reads back a value the implementation printed:

1. **Closed form.** The analysis pulse peaks at `Ninv − 1` with a positive sign.
2. **Closed form.** `Δt_N = −L·ln N` for N = 2 and 3 against a synthetic
   memoryless nonlinearity. Tolerance **±3 samples, not ±1**: the residual is
   config-dependent (0.0 and −0.1 samples for a 20 Hz–20 kHz sweep, +2.44 and
   −1.18 for 100 Hz–10 kHz) until Novak synchronisation is adopted. Two orders,
   so the assertion is about logarithmic spacing rather than one offset.
3. **Round trip, two shapes and two windows.** A pure delay must come back at
   the exact index with the exact gain. A 4th-order 300 Hz highpass must match
   its analytic magnitude to better than 0.1 dB over 100 Hz – 10 kHz **with**
   decision 7's lead-in, and must be visibly wrong without it. The second half
   of that is the test that would have caught the naive window; a round-trip
   test that only checks the peak passes a broken one.
4. **Derived band.** A unity path reads flat across `[f_lo, f_hi]` to the
   deviation decision 4 tabulates for the configuration under test — ±0.03 dB at
   2.00/0.50 octaves. Asserting a tighter figure than the configuration can
   deliver would be asserting the fade widths, not the normalisation.
5. **Regression lock, labelled as one.** The pre-arrival floor at
   `fadeInOctaves = 2` is at or below −100 dB, and at 0.5 is worse than −80 dB.
   These bracket the measured law in decision 5; they are locks against an
   empirical table, not a closed form, and `tools/probe_sweep_fade.py` is
   committed so the table can be re-derived rather than trusted.
6. **Polarity, on both sides of the gate — REWRITTEN 2026-08-30 with decision
   6b.** The bandwidth boundary this item used to describe does not exist; see
   6b. What CI must assert now, with `+s` yielding `+1` and `−s` yielding `−1`:

   - **Inside the box** (measured low edge ≤ 100 Hz *and* high edge ≥ 8 kHz): a
     full-range passband answers, and so do a maximum-phase pair, an allpass, and
     an inverted reflection louder than the direct sound.
   - **Outside it from below**: a 30–120 Hz subwoofer yields `Unknown` with
     `BandTooHigh` — its band stops too low.
   - **Outside it from above, and this is the case with teeth**: a 1000–16000 Hz
     order-8 horn yields `Unknown` with `BandTooLow`. It spans **four octaves**,
     so every bandwidth-in-octaves gate ever proposed would admit it, and
     measured it answers *backwards*. A suite without this case passes an
     implementation that sends an operator to rewire a working loudspeaker.

   Note what this suite may **not** assert: that a subwoofer yields a sign.
   An earlier draft asserted exactly that alongside "narrowband yields Unknown",
   which are only simultaneously satisfiable for one hand-picked subwoofer
   fixture. A test suite whose two cases can only both pass for one choice of
   fixture is testing the fixture.

   And what it must assert about the gate itself: that widening `gateLowHz`
   turns the horn case from `Unknown` into a **wrong sign**. A gate is only
   proven by showing what goes wrong without it — a gate that refuses everything
   also produces a green suite.
7. **Capture length.** A synthetic room with a known RT60, deconvolved from a
   capture truncated at `gap = 0.5 × RT60`, must show a worst-bin error above
   1 dB, and at `1.5 × RT60` must show one below 0.1 dB. Asserting only the
   second would pass an implementation that never noticed the first.
8. **Golden vector.** `tools/gen_golden.py` gains a case generated with
   numpy/scipy in the checkout's `.venv`.

Plus one count that must be *read*, not assumed: `core_has_no_framework_deps`
scans 70 files today, and must scan 70 + n after this lane. Trap #1 in
`docs/HANDOFF.md` is that a mis-globbed guard passes while watching nothing.

## What this record does not decide

- **ETC, Schroeder integration, Lundeby truncation, RT60, clarity.** L4b.
  One thing L4a must hand it, measured here so it is not rediscovered as a
  bug: Müller warns that linear deconvolution turns steady noise into a
  decaying, progressively low-pass filtered tail that reads as reverberation.
  With white noise at about −34 dB added to the captured sweep
  (`verify_l4a_measurement.py` section G), 100 ms slices of the deconvolved result:

  | after t = 0 | level | spectral centroid |
  |---|---|---|
  | 100 ms | −78.1 dB | 7722 Hz |
  | 400 ms | −87.0 dB | 3239 Hz |
  | 800 ms | −99.2 dB | 1322 Hz |
  | 1400 ms | −117.0 dB | 1605 Hz |

  **A decay of 30.0 dB/s whose centroid falls with it, produced by a
  measurement of nothing at all.** Read as a room it reports RT60 = 2.00 s,
  shortening with frequency — an entirely plausible answer with no room in it.
  (The centroid rising again at 1400 ms is the level reaching the arithmetic
  floor, not a feature.)

  **And the rate is not accidental.** The inverse filter's +6 dB/oct envelope
  spans exactly `20·log10(f2/f1)` dB across the sweep's length, and the
  deconvolved noise inherits that envelope, so

      apparent RT60 ≈ 3·T / log10(f2/f1)

  For the case above — three decades in 2 s — that is `RT60 = T` exactly, which
  is the sort of coincidence that gets promoted to a law. Tested against two
  further configurations to stop that happening: T = 4 s over three decades
  predicts 4.00 s and measures 3.58 s (−10.5%); T = 2 s over two decades
  predicts 3.00 s and measures 3.21 s (+6.8%). **The mechanism is derived, the
  magnitude is good to about 10% over that range, and the residual is
  unexplained.** L4b should use it to *recognise* the artefact, never to
  subtract it — and must find its own truncation point rather than trust the
  visible decay.
- **The draggable gate, min/excess phase, offline WAV.** L4c — and one hazard
  that lane will meet and this one never does. **Clock drift.** When the device
  playing the sweep and the device recording it do not share a clock, the
  captured response is time-scaled, and the recovered impulse response skews:
  the tail smears, high frequencies worst. A single duplex interface is safe by
  construction, which is why L4a can ignore it. **G22 — offline dual-FFT against
  a WAV recorded elsewhere — is the textbook exposure**, and a USB microphone
  running a few thousand ppm off is ordinary rather than exotic. Farina's 2007
  paper devotes a section to it and REW ships a clock-adjust control driven by a
  loopback or acoustic reference. L4c must decide whether to detect it, correct
  it, or refuse the file; it must not discover it.

- **~~What G21 promises an operator about a subwoofer~~ — DECIDED 2026-08-30,
  then AMENDED the same day. See decision 6b.** The three tiers as first
  approved: absolute polarity keeps the 2.5-octave gate but every `Unknown`
  carries its reason; RELATIVE polarity becomes the real answer for a subwoofer
  in L4b/L4c; a guided "main then sub" workflow goes to L7 beside G17.

  **Two of those three did not survive being measured, and the owner approved
  the amendments in the building session on 2026-08-30:**

  1. The 2.5-octave gate is gone. It was the floor of a one-family survey.
     Decision 6b replaces it with a band-edge box.
  2. The parenthetical below — that cross-correlation against a stored trace
     "is sound at any bandwidth" — is **true only for the same system measured
     twice**. Across two systems with different passbands it is false, measured:
     a correctly wired sub/main pair reads the opposite sign at crossover orders
     2 and 4, and the sign flips again at orders 1 and 8. Linearity proves
     "negating the drive negates the response"; it does not prove anything about
     comparing *different* responses.

     **Attributed 2026-09-15, and the finding survives intact** —
     `docs/research/2026-09-15-l7-align-order4-probe.md`. That order pattern is
     reproduced exactly by two **band-pass** boxes correlated against each other
     (a band-passed sub against a band-passed main) and by no matched-cutoff
     complementary crossover pair, which follows the closed form `N·90°` at
     every order instead. So the sentence above is right about what it measured
     and right about why — *different passbands* is the operative phrase, not
     *different orders*. What it must NOT be read as is a property of
     crossovers: a matched pair reads wrong at 2 and **right** at 4, and the
     value at lag 0 is positive at order 4 even in the band-passed case. The
     mechanism is that the peak leaves lag 0 onto an oppositely-signed lobe once
     the passbands differ, which is a limit of the **peak-sign rule**, not of
     the pair. L7-ALIGN §3 and §13.1 carry the ruling.
  3. Tier 3 therefore moved: the guided sub-against-main workflow belongs to
     G17 as a **phase** question, because the correct phase offset at a
     crossover is set by the intended topology — 0° for Linkwitz-Riley, 180° for
     2nd-order Butterworth, 90° for odd-order Butterworth — and no measurement
     recovers the designer's intent.

  Relative polarity still ships, and is still the right answer for a subwoofer
  compared against **itself** before and after a change. It needs its own
  refusal gate: a normalised cross-correlation ρ = |peak| / √(E₁E₂), bounded in
  [0, 1] by Cauchy–Schwarz — which is the property the three dead gate variables
  all lacked. Measured to survive 20 dB SNR, a 10 ms offset, unit spread, and a
  room down to a direct-to-reverberant ratio of **−6 dB**, below which it
  refuses rather than lies (the sign stays correct throughout; only ρ falls
  under the bar).

  **These ρ figures come from ONE grid and have not been reproduced
  independently** — unlike every number in decision 6b, which three sessions
  built separately. A 30 ms offset was measured clean in a separate run without
  a room, and is not part of the combination above. L4b's step 0 must re-derive
  all of this before any ρ threshold ships. Do not let one-source numbers wear
  the uniform of three-source ones.

  What follows is the analysis the original decision was made from, kept as
  history. **Its 2.5-octave figures are superseded.**

  This is a product decision, not a DSP one, and it is the sharpest consequence
  of decision 6.
  A 30–120 Hz subwoofer is **2 octaves**, below the 2.5-octave minimum, so the
  polarity checker will return `Unknown` for it — **and a subwoofer is what a
  live-sound operator reaches for a polarity checker to settle.** The refusal is
  honest: at 2 octaves the measurement agrees with the drive polarity at centres
  45–120 Hz and disagrees at 30 Hz, with nothing distinguishing the two cases on
  screen. Shipping the sign anyway would be right most of the time and
  confidently wrong the rest, which is the worst available behaviour for a
  reading that sends someone to rewire a box.

  **The way out is relative polarity, and it belongs to L4c.** Comparing two
  measurements — this box against that one, or the same box before and after —
  is sound at any bandwidth, because negating the drive negates the response
  exactly. If L4c's interface offers no relative comparison, G21 is close to
  useless for its most common case. That is the owner's call about what the
  feature *is*, and the number it hinges on is 2 octaves versus 2.5.

- **Whether to adopt Novak's synchronized sweep length** (decision 3's
  qualifier). Cost today: one `round()` in the generator and a regeneration of
  the sweep golden vectors, changing `durationSec` by under 1%. Cost of
  deferring: phase-accurate per-harmonic distortion analysis is unavailable to
  G3/P4b until every capture is retaken. **This is an owner call, not an
  implementation detail**, because it trades a small change now against a class
  of measurement later.

- **Whether to sweep to Nyquist and drop the fade-out entirely.** Farina (2007)
  notes that high-frequency pre-ringing comes from an improper fade-out, and
  that a sweep running to Nyquist needs none. At 48 kHz that means `endHz`
  24000 rather than 20000. It points the same way as decision 5's measurement —
  a wider fade-out is actively harmful — and would remove a variable rather than
  tune it. Unmeasured here.
- **STI/STIPA.** Blocked on IEC 60268-16.
- **THD from the harmonic packets.** G3/P4b. L4a only guarantees the packets
  survive and that `harmonicSpacingL` locates them.
- **Whether the app offers a loopback reference measurement** (Müller's
  preferred method). That needs M7's cable to exist first, and decision 2 is
  what makes adopting it cheap when it does.
