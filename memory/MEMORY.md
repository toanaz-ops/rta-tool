# RTA Tool — memory index

One line per memory. Content lives in the linked file, never here.

- [ASIO SDK is GPLv3 since Oct 2025](asio-sdk-is-gplv3-since-oct-2025.md) — the 20-year barrier to open-source ASIO fell right before this project started
- [JUCE is AGPLv3, not GPLv3](juce-is-agplv3-not-gplv3.md) — why this project is AGPL-3.0 and what that costs later
- [core/ must not include frameworks](core-must-not-include-frameworks.md) — the architectural rule, and the test that enforces it
- [Build toolchain on this machine](build-toolchain-on-this-machine.md) — MSVC 2026 lives in an unusual path; the venv (main checkout only, not worktrees) has numpy and scipy
- [RealFft is single precision](float32-fft-precision.md) — why a 1e-9 tolerance on any FFT-derived value fails against correct code, and what shape a correct tolerance has
- [Dual-FFT conventions that must not be tidied](dual-fft-conventions.md) — the four sign/definition choices a later reader will be tempted to flip, and what flipping each one breaks
- [Re-verify what the change could have changed](reverify-what-the-change-could-have-changed.md) — after a merge, a name-only diff can prove a rebuild is unnecessary; and when it cannot, it tells you which configuration to run
- [A fixture can be too well-behaved to fail](a-fixture-can-be-too-well-behaved-to-fail.md) — the capture-length test read 0.0026 dB and could not have failed; three fixes later it read 1.76 dB
- [A threshold read off a grid is that grid's floor](a-threshold-read-off-a-grid-is-that-grids-floor.md) — three thresholds, three sessions, each the floor of its own grid; what to ask before adopting a fourth, and when a scalar gate does survive
- [A fixed defect returns through the silent fallback](a-fixed-defect-returns-through-the-silent-fallback.md) — the branch where the fix does not run quietly restores the bug, and no fixture is short enough to notice
- [The venv imports acoustics only through a shim](the-venv-imports-acoustics-only-through-a-shim.md) — scipy 1.18 removed sph_harm; the shim makes imports live and real sph_harm calls silently wrong; pyrato is named in CLAUDE.md but not installed
- [A gen script runs the moment you invoke it](a-gen-script-runs-the-moment-you-invoke-it.md) — no argparse anywhere in tools/gen_*.py; --help ran the pipeline and overwrote a golden, byte-identical only because generation is deterministic
- [A public issue has a date too](a-public-issue-has-a-date-too.md) — an issue described a convergence bug the current code no longer has; read the code at a pinned commit before citing the issue
- [Một biện pháp chống bẫy mang sang phải tự biện minh lại](an-anti-trap-measure-must-re-earn-its-place.md) — khe 6 ms và cửa sổ 20 ms; +0.4% thành +17.2%.
- [Hai bản dựng lệch nhau không chứng minh bên nào sai](two-builds-disagreeing-is-not-evidence-one-is-wrong.md) — bốn vòng truy nhiễu lấy mẫu của median-24; và vì sao cực trị là thống kê yếu nhất trước mẫu nhỏ.
- [Câu trả lời của chủ nhân ngắn hơn câu hỏi là câu mơ hồ](an-owner-reply-shorter-than-the-question-is-ambiguous.md) — `b:` bị đọc thành phương án (b) và ghi vào queue như quyết định.
- [Một build cấu hình sai đi 99% quãng đường rồi hỏng như một test thường](a-misconfigured-build-goes-99-percent-of-the-way.md) — Ninja chọn MinGW, 196/196 object sạch, chết ở test discovery.
- [A default must be run through the gate it feeds](a-default-must-be-run-through-the-gate-it-feeds.md) — 0.5 s uniform-seconds averaging left three MTW bands under the coherence gate forever; the header formula said fine, the shipped function said no.
- [A header ceiling is not the reachable average count](a-header-ceiling-is-not-the-reachable-average-count.md) — exponentialEffectiveAverages never reaches (2-a)/a; the overlap penalty D divides it to 17.1123296 at alpha=1-exp(-1/16), not ~32.
- [Positions are not replicates](positions-are-not-replicates.md) — inverse-variance weighting is optimal only for replicate estimates of one quantity; across mic positions it lets one clean mic own the average 99:1, so L6b shipped the bounded `u·γ²`.
- [A placeholder for an absent result erases its state](a-placeholder-for-an-absent-result-erases-its-state.md) — the MTW combine substituted a default band for a `nullopt` band and rewrote `NoWeight/2` as `NoContributor/0`; no fixture had reached it.
- [The parity table is a hypothesis](the-parity-table-is-a-hypothesis.md) — three L6b premises from the gap table were wrong (REW weighting, SysTune SSA, M1 discard); station 1 opens with corrections to the brief.
- [A shared kernel's numeric factor is not transferable](a-shared-kernels-numeric-factor-is-not-transferable.md) — min-phase 8× (FIR, windowed) vs 64×/128× (EQ G24, raw magnitude); the magnitude-identity residual is a tautology, not evidence.
- [Mutation testing needs the exe deleted first](mutation-testing-needs-the-exe-deleted-first.md) — cmake --build can log "-> X.exe" without relinking; a stale binary false-PASSes mutations. Two more staleness modes since: a header mutation is not recompiled at all unless a dependent `.cpp` is touched, and RESTORING a header leaves objects built from the mutated one — finish with a full rebuild and hash-check against HEAD.
- [A cap checked on the drain path is unchecked on the publish path](a-cap-checked-on-the-drain-path-is-unchecked-on-the-publish-path.md) — the drain guarded route positions at 8; membership did not, admitting routes 9+ and over-reading an 8-slot analysers[] on a >8-channel interface.
- [A convention cannot be blamed for a value it maps to itself](a-convention-cannot-be-blamed-for-a-value-it-maps-to-itself.md) — conjugation negates a phase and negation fixes 0° and 180°, so no dual-FFT sign flip can reach an even-order crossover; check a suspect's reachable set before building the grid
- [A tolerance inherited from a plan is that plan's fixture](a-tolerance-inherited-from-a-plan-is-that-plans-fixture.md) — 1e-12 on a filterbank is 1.23e-12 on a high-Q dip; 0.7 and 12.4 are not `float`; derive the bound in the fixture and it comes out TIGHTER
- [An ambiguity spacing comes from the band's WIDTH, never from its mean](an-ambiguity-spacing-comes-from-the-bands-width.md) — 8.26 ms vs 10.0 ms; two windows of one width at different centres settle it where no tolerance could
- [A plan that names both the check and the mutation has still not run them against each other](a-prescribed-mutation-is-not-proof-the-check-catches-it.md) — I4's grep stayed green under the mutation I4 itself prescribes; when a word list cannot do the job enumerate what is ALLOWED, and write a structural check's SCOPE into the artefact and check the comment's own example against it — five turns, one lost to a newline and one to a URL in a string, because a premise no test enforces is a hole with a caption **Attribution variant (2026-09-18, paid for twice in one PR)**: a guard that works but is CREDITED with a property it never touches is worse than none, because it stops anyone looking for the real one -- both times the tell was a claim made from where a value is DECLARED rather than where it is USED.
- [A hidden Catch2 tag is not a gate](a-hidden-catch2-tag-is-not-a-gate.md) — `[.]` hides a case from the DEFAULT run only; `exe "[api]"` reached a destructive regenerator, rewrote the golden mid-suite, and the next identical run passed — a gate must be something a tag expression cannot supply, asserted by a test that runs under the filter that used to trip it
- [A shortest decimal is not a fixed point of narrowing](a-shortest-decimal-is-not-a-fixed-point-of-narrowing.md) — a plan row asked that every wire number satisfy `(double)(float)v == v`; shortest-round-trip emission refuses to produce such a value, so the two tasks contradicted each other and only building both showed it
- [A bitwise identity can belong to the ISA, not the arithmetic](a-bitwise-identity-can-belong-to-the-isa-not-the-arithmetic.md) — Apple arm64 contracts `10*log10(p)+k` into one `fma`; four macOS-only reds, 35 golden bytes at 1 float ULP, and ubuntu==windows is what ruled libm out
- [An allocation the optimiser removed reads as zero bytes](an-allocation-the-optimiser-removed-reads-as-zero-bytes.md) — clang elides libc++'s `__builtin_operator_new` for a buffer that never escapes; the probe read 0 >= 8192 and every sibling assertion was too weak to tell blind from elided
- [A verifier with Bash can git-checkout your uncommitted fix](a-verifier-with-bash-can-git-checkout-your-uncommitted-fix.md) — no Write tool still leaves git; `git checkout -- file` on an uncommitted fix resets it to HEAD; commit before dispatching, and rebuild from the current tree before trusting a reported GREEN. **Not only a verifier**: a single builder reverting its own mutation probe loses the fix underneath it the same way.
- [A config field with one value in every fixture is untested](a-config-field-with-one-value-in-every-fixture.md) — `SplConfig::metrics` carried a per-metric weighting and `SplSession` built ONE A-weighted meter, so the field was read and thrown away; count the instances the implementation constructs against the cardinality the config can express.
- [A test convenience can move coverage off the shipped branch](a-test-convenience-can-move-coverage-off-the-shipped-branch.md) — twelve socket cases bound an ephemeral port for good stated reasons; the fixed-port branch the product takes ran zero times, and no mutation of it could go red
- [A blocker in the queue has a date too](a-blocker-in-the-queue-has-a-date-too.md) — the `[!]` "Actions is billing-blocked" was repeated by four PR bodies for a day after it lifted, so four PRs merged against a live matrix that was RED on macOS; re-run the cheapest refuting command before citing a recorded blocker
- [A filter named after the lane may select none of the lane's tests](a-filter-named-after-the-lane-may-select-none-of-it.md) — `ctest -R "api"` selects one test and it is a guard; 0 of 837 `TEST_CASE` names carry a lowercase `api` and the 26 socket cases carry no tag at all, so count what a filter selects and paste the count
